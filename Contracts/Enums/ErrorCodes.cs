namespace Contracts.Enums
{
    public class ErrorCodes
    {
        public enum enErrorCodes
        {
            INVALID_DATA,
            INVALID_AMOUNT,
            INVALID_ID,
            NOT_FOUND,
            AUTHENCATION_FAILED,
            AUTHORIZE_FAILED,
            BUSINESS_RULE_VIOLATION,
            DB_ERROR,
        }
    }
}